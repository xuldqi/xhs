import type { AccountData, GuideContent } from '@/types'
import { SECTION_TITLES } from '@/types'

export const demoAccountData: AccountData = {
  username: '小红书运营达人',
  followerCount: 156,
  postCount: 23,
  contentCategory: '知识分享',
  recentPosts: [],
  analysisDate: new Date()
}
